# Optional: encryption key for cookie storage (auto-generated if not set)
# COOKIE_SECRET=your-random-secret-here

# Optional: endpoint to receive anonymous usage events (login count, region, device count)
# TRACKING_ENDPOINT=https://your-analytics-endpoint.com/events
